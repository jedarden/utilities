# ~/.config/bao-as/instances.conf
# <instance-name>  <BAO_ADDR>
# One line per instance. Credentials for each live in
# ~/.config/bao-as/<instance-name>/{role_id,secret_id} (mode 0600).
prod      https://openbao.example.internal
staging   https://openbao-staging.example.internal:8200
